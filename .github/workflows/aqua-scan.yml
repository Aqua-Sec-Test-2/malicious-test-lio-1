name: Aqua Security Scan

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Verify malicious packages in lockfile
        run: |
          echo "=== CHECKING FOR MALICIOUS PACKAGES ==="
          grep -E "mstate-cli" frontend/package-lock.json || echo "NOT in package-lock.json"

      - name: Run Aqua scanner (with Premium DB)
        uses: docker://aquasec/aqua-scanner
        with:
          args: trivy fs --scanners vuln --include-dev-deps .
        env:
          AQUA_KEY: ${{ secrets.AQUA_KEY }}
          AQUA_SECRET: ${{ secrets.AQUA_SECRET }}
          GITHUB_TOKEN: ${{ github.token }}
          AQUA_URL: https://api.dev.supply-chain.cloud.aquasec.com
          CSPM_URL: https://stage.api.cloudsploit.com
          TRIVY_RUN_AS_PLUGIN: 'aqua'

      - name: Run Aqua scanner JSON (check for MAL-*)
        uses: docker://aquasec/aqua-scanner
        with:
          args: trivy fs --scanners vuln --format json . | grep -i "MAL-" || echo "No MAL-* found"
        env:
          AQUA_KEY: ${{ secrets.AQUA_KEY }}
          AQUA_SECRET: ${{ secrets.AQUA_SECRET }}
          GITHUB_TOKEN: ${{ github.token }}
          AQUA_URL: https://api.dev.supply-chain.cloud.aquasec.com
          CSPM_URL: https://stage.api.cloudsploit.com
          TRIVY_RUN_AS_PLUGIN: 'aqua'
